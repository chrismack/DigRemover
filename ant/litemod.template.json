{
    "name": "${ant.project.name}",
    "mcversion": "${litemodversion}",
    "classTransformerClasses" : [
        "com.chris.dig.DigRemoverTransformer"
    ],
    "revision": "${build.number}",
    "author": "${author}",
    "description": "Removes particles when blocks are broken!"
}
